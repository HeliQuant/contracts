# Foundry build artifacts (regenerated by forge build)
cache/
out/

# Forge install destinations (regenerated by forge install)
lib/

# Broadcast outputs except the canonical run-latest of Mantle Sepolia deploy
# (keep deployments/ — it's the canonical record of deployed addresses)
broadcast/

# Environment
.env
.env.local
*.env

# IDE
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
*.log
